import { NotificationTitle } from '../components/FeedbackAlertMessage/NotificationAlertMessage.style';

const Notification = ({ message }) => {
  return (
    <NotificationTitle>{ message}</NotificationTitle>
    )
  }
  export default Notification
